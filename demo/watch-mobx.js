import {
    makeAutoObservable,
    makeObservable,
    observable,
    action,
    computed,
} from "mobx";
import { observer } from "mobx-react-lite";
export {
    observer,
    makeAutoObservable,
    makeObservable,
    observable,
    action,
    computed,
};
