import {
    makeAutoObservable,
    makeObservable,
    observable,
    action,
    computed,
    Reaction,
} from "mobx";
// import { Observer } from "mobx-react-lite";
import { observer, Observer } from "../my-mobx";
export {
    Observer,
    observer,
    makeAutoObservable,
    makeObservable,
    observable,
    action,
    computed,
    Reaction,
};
